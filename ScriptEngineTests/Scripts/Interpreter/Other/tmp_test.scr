Процедура Тест()
	значение1 = истина;
	значение2 = Ложь;
    ф = 0;
	Пока значение1 и ф <> 5 Цикл
 		Сообщить(ф);
		ф = ф+1;
	конеццикла;

КонецПроцедуры

Процедура ТестДолжен_ПроверитьЧтоПослеЛогическогоОператораОбрываетсяЦепочкаПриОперацииСНизкимПриоритетом_Ошибка184()

	Номер = 0;

	Массив = Новый Массив;
	Массив.Добавить("foo");
	Массив.Добавить("bar");

	ЭлементМассива = Массив[0];

	Сообщить(ВРег(ЭлементМассива) = ВРег("path83") И Номер+1<=100 или Истина);

КонецПроцедуры


Функция ТестоваяПроцедура_УстановитьПоСсылке(Параметр)
	Параметр = "НОВОЕЗНАЧЕНИЕ";
	Возврат Истина;
КонецФункции

Процедура ТестДолжен_ПроверитьСокращенноеВычислениеПоИЛИ() Экспорт

	значение1 = истина;
	значение2 = Ложь;
	А = 5;
	Если значение1 или значение1 и значение2 или ТестоваяПроцедура_УстановитьПоСсылке(А) Тогда
	КонецЕсли;

	Сообщить(А);

КонецПроцедуры


Процедура ТестДолжен_ПроверитьСокращенноеВычислениеПоИ() Экспорт

	значение1 = истина;
	значение2 = Ложь;
	А = 5;
	Если значение2 или значение1 и значение2 И ТестоваяПроцедура_УстановитьПоСсылке(А) Тогда
	КонецЕсли;

	Сообщить(А);

КонецПроцедуры

Тест();
ТестДолжен_ПроверитьЧтоПослеЛогическогоОператораОбрываетсяЦепочкаПриОперацииСНизкимПриоритетом_Ошибка184();
ТестДолжен_ПроверитьСокращенноеВычислениеПоИЛИ();
ТестДолжен_ПроверитьСокращенноеВычислениеПоИ();